_schema-version: 3.3.0
ID: dhi_cms
description: A simple CAP project.
version: 1.0.0
modules:
  - name: dhi_cms-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: dhi_cms-db
      - name: uaa_dhi_cms
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: dhi_cms-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: dhi_cms-db
    parameters:
      buildpack: nodejs_buildpack
  - name: dhi_cms-destination-content
    type: com.sap.application.content
    requires:
      - name: dhi_cms-destination-service
        parameters:
          content-target: true
      - name: dhi_cms_html_repo_host
        parameters:
          service-key:
            name: dhi_cms_html_repo_host-key
      - name: uaa_dhi_cms
        parameters:
          service-key:
            name: uaa_dhi_cms-key
    parameters:
      content:
        instance:
          destinations:
            - Name: cms_dhi_cms_html_repo_host
              ServiceInstanceName: dhi_cms-html5-app-host-service
              ServiceKeyName: dhi_cms_html_repo_host-key
              sap.cloud.service: cms
            - Authentication: OAuth2UserTokenExchange
              Name: cms_uaa_dhi_cms
              ServiceInstanceName: dhi_cms-xsuaa-service
              ServiceKeyName: uaa_dhi_cms-key
              sap.cloud.service: cms
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
  - name: dhi_cms-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: dhi_cms_html_repo_host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - comdhicmscmsadmin.zip
          name: comdhicmscmsadmin
          target-path: resources/
        - artifacts:
            - comdhicmscmsrequester.zip
          name: comdhicmscmsrequester
          target-path: resources/
  - name: comdhicmscmsadmin
    type: html5
    path: app/cms_admin
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: comdhicmscmsrequester
    type: html5
    path: app/cms_requester
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: dhi_cms-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: dhi_cms-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
              - Authentication: NoAuthentication
                HTML5.DynamicDestination: true
                HTML5.ForwardAuthToken: true
                Name: srv-api
                ProxyType: Internet
                Type: HTTP
                URL: ~{srv-api/srv-url}
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: dhi_cms-destination-service
      service-plan: lite
    requires:
      - name: srv-api
  - name: dhi_cms_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: dhi_cms-html5-app-host-service
      service-plan: app-host
  - name: uaa_dhi_cms
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: dhi_cms-xsuaa-service
      service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
